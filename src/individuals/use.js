const
    _ = require('../util'),
    model = require('../model');

/**
 * To use the Asset
 */
module.exports = new model.Action(
    { '@id': _.ODRL.use },
    async function (...args) {
        // TODO
    }
);